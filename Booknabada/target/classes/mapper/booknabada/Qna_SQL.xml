<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="qna">

	<select id="board" resultType="com.booknabada.dto.QnaDTO">
		SELECT board_no, board_title, board_date, board_count, user_name,
		(SELECT COUNT(*) FROM qnaBoard_view) AS totalCount
		FROM qnaBoard_view limit #{page }, 11
	</select>
	
	<select id="detail" parameterType="Integer" resultType="com.booknabada.dto.QnaDTO">
		SELECT * FROM qnaBoard_view WHERE board_no = #{reBno}
	</select>
	
	<update id="countUp" parameterType="Integer">
	<![CDATA[
		UPDATE board SET board_count = board_count + 1 WHERE board_no = #{reBno}
		]]>
	</update>
	
	<insert id="writeAction" parameterType="com.booknabada.dto.QnaDTO">
		<![CDATA[
			INSERT INTO board (board_title, board_content, cate_no, user_no, board_picture)
			VALUES(#{board_title}, #{board_content}, 1 , 1 , #{board_picture }) 
		]]>
	</insert>
	
	<delete id="detailDelete" parameterType="com.booknabada.dto.QnaDTO">
		DELETE FROM board WHERE board_no = #{board_no} 
	</delete>
	
	<update id="modifyAction" parameterType="com.booknabada.dto.QnaDTO">
		<![CDATA[
		UPDATE board SET board_title = #{board_title}, board_content = #{board_content}, board_picture = #{board_picture}
		WHERE board_no = #{board_no}
		]]>
	</update>

		
</mapper>